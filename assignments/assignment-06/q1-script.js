const fruits =["Apple", "Banana","Graps", "Pineapple", "Pomegranate"];

// to display the number of elements
console.log(fruits.length);

// to convert the array into a string
console.log(fruits.toString());

// to access at least two elements using indexes
console.log(fruits.at(1));
console.log(fruits.at(3));

// to display the elements separated by a symbol or text
console.log(fruits.join(""));
console.log(fruits.join("-"));